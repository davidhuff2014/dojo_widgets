require 'spec_helper'

describe DojoWidgets do
  it 'has a version number' do
    expect(DojoWidgets::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
